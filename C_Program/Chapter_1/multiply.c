#include<stdio.h>

int main(){
    int a, b, multiply;
    printf("Enter the value of a is :");
    scanf("%d",&a);
    printf("Enter the value of b is :");
    scanf("%d",&b);
    multiply=a*b;
    printf("The value of a*b is %d",multiply);
    return 0;
}